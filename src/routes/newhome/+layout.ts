import type { LayoutLoad } from './$types';

export const load: LayoutLoad = () => {
  return {
    meta: {
      title: 'NewHome',
      description: 'NewHome | A New Home for your browser',
      url: 'https://electris.net/newhome'
    }
  };
};
